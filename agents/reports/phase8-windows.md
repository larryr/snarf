# Phase 8 report — windows & chrome

Merged to `main`. Contracts: agents/contracts/phase8-{windows,chrome,routing}.md
(rulings R-P8-1..13). Suite: **317/317**; smoke 14/14; boundary verified.

## Delivered — Snarf looks like acme

The full chrome: Row (with its "Newcol Kill Putall Dump Exit " tag strip), Columns
(with "New Cut Paste Snarf Sort Zerox Delcol " tags and purple buttons), Windows
(pale-blue editable tag + button showing dirty state + purpleblue divider + body),
live scrollbar strips (elevator math + B1/B2/B3 click actions — the phase-7
deferral retired), and multi-window routing. Every tag is a full editable Text over
its own File. core/boot.zig born (the acme.c-main analog) — one assembly point for
main_wasm and acceptance.

## The headline ruling (R-P8-9 — Larry can veto with one word)

**POINT-TO-TYPE**, acme's authentic default: keys go to the text under the mouse
pointer (rows.c:279-282 verified — click-to-type is only acme's -b flag). O17
recommended modernizing to click-to-type; overruled on the fidelity principle, with
O16 concurring. The -b variant is one line away if wanted.

## Other verified acme facts

- Wheel scrolls the text under the pointer (acme.c:618-629), never a "focus".
- The exact palette derived from allocimagemix math (tag pale blue 0xEAFFFF, body
  ivory 0xFFFFEA, yellowgreen body borders — main_wasm's border color was corrected).
- Mouse warping is a PERMANENT divergence (browsers cannot warp the pointer);
  acme's warp-dependent niceties dropped with citations.
- One sanctioned hash re-freeze (R-P8-12) — spot-checks verified before and after;
  the accept-scene hashes survived untouched (backfill paints same-color).

## Deferred

Drag-resize (coldragwin/rowdragcol/colgrow — phase-9-adjacent); tag expand/multi-line
tags; winsettag live recomposition + parsetag (phase 9 with exec); colclose/Del
(phase 9); Sort/Zerox; canvasResize.
